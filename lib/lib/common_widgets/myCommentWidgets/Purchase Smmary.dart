import 'package:flutter/material.dart';

class PurchaseSmmary extends StatelessWidget {
  const PurchaseSmmary({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text("data"),
    );
  }
}
